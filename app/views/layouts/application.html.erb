<!DOCTYPE html>
<html>
  <head id="head">
    <title>RailsCrowdKaraoke</title>
    <%= link_to favicon_link_tag("/assets/disco-icon.png", rel: "icon", type: "image/png") %>
    <!-- <link rel="icon" type="image/png" href="/assets/disco-icon.png"> -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Amaranth:ital,wght@0,400;0,700;1,400;1,700&family=Roboto:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
  </head>

  <body class="<%= "#{controller_name}-#{action_name}" %> <%= @winning_genre.slug.downcase if @winning_genre %>">
    <%= render "shared/navbar" %>
    <%= render "shared/flashes" %>

    <%= yield %>
  </body>
</html>
